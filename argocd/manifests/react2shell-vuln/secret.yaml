# Pod-tier crown jewel: mounted into the vuln pod as an env var.
# Reachable by RCE alone (CVE-2025-55182). Dummy value — lab only.
apiVersion: v1
kind: Secret
metadata:
  name: secret-pod-vuln
  namespace: vuln
type: Opaque
stringData:
  pwd: "pod-hello-world"
